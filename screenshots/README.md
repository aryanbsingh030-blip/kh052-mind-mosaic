# Interface Previews

`screenshot-1.png` and `screenshot-2.png` are static interface previews created from the implemented Dashboard and Team Builder component hierarchy, labels, capabilities, and visual system. They are included to make the judging package easy to review without installing dependencies. They are not represented as live runtime captures.
